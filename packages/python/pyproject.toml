[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "poder"
version = "1.0.0"
description = "Lean Chilean RUT validation and formatting for Python"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "enemigos" }]
keywords = ["rut", "chile", "validate", "format"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/enemigos/poder"
Repository = "https://github.com/enemigos/poder"
Issues = "https://github.com/enemigos/poder/issues"

[project.optional-dependencies]
dev = ["pytest>=8.0", "mypy>=1.14", "ruff>=0.9"]

[tool.hatch.build.targets.sdist]
include = ["src/poder", "README.md", "LICENSE"]

[tool.hatch.build.targets.wheel]
packages = ["src/poder"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "A", "RUF"]
# `input` and `format` shadow builtins on purpose: the public API mirrors the
# TypeScript package, where `format` and the `input` issue field are the names
# users already know.
ignore = ["A001", "A002", "A004"]

[tool.ruff.format]
# Documentation code blocks use aligned trailing comments on purpose.
exclude = ["*.md"]

[tool.mypy]
python_version = "3.10"
strict = true
mypy_path = "src"
packages = ["poder", "tests"]
